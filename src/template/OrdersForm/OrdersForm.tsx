export default function OrdersForm() {
	// const { data, loading } = useFetchAuth<IResponse<IOrders>>({
	// 	endpoint: 'auth/profile-orders/10',
	// 	options: {
	// 		method: 'GET',
	// 	},
	// });
	// if (loading) return <div>Loading...</div>;
	// console.log(data);
	// return (
	// 	<div className={styles.wrapper}>
	// 		{data && data?.items?.length > 0 ? (
	// 			<DashboardTableOrders type={'profile'} orders={data} />
	// 		) : (
	// 			<EmptyDataBlock />
	// 		)}
	// 	</div>
	// );
	return <div>Dosen`t work have bug in backend</div>;
}
